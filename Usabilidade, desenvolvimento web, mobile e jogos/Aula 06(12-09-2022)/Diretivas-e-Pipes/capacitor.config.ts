import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Diretivas-e-Pipes',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
